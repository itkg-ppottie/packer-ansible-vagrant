- include: postgresql.yml
  when: world == "vm-local"
  

# create overlay networks
- hosts: managers[0]
  roles:
    - network

# Deploy project
- hosts: managers[0]
  roles:
    - role: api-kilometers
      PROJECT_VARS_FILE: "../../../../../../configs/{{ world }}/api-kilometers/vars.yml"
      APP_VERSION: "1.0.4rc2"

